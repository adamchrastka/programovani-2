const button = document.getElementById('vypisBtn'); 
button.addEventListener('click', function() {
    var retezec = document.getElementById("retezecInput").value;
    let delka = retezec.length;
    console.log(delka);
});